{
  "name": "wedding-invitation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages -d .output/public",
    "generate-and-deploy": "yarn generate && yarn deploy"
  },
  "dependencies": {
    "nuxt": "^3.17.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@nuxt/image": "^1.9.0",
    "gh-pages": "^6.1.1"
  }
}
